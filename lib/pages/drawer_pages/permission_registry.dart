import 'package:flutter/material.dart';

class PermissionRegistry extends StatelessWidget {
  const PermissionRegistry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Registro de permisos"),
    );
  }
}
