import 'package:flutter/material.dart';

class PermissionRequests extends StatelessWidget {
  const PermissionRequests({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Solicitudes de permiso"),
    );
  }
}
